node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

.vercel
.freebuff
*.apk

# Local exercise video library (not part of the source repo)
Videos/

# bun lockfile (repo uses npm)
bun.lock

# Android / Gradle build output
android/.gradle/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/build/
android/local.properties
*.keystore
*.jks

# Credenciales de firma del release (usar keystore.properties.example)
android/keystore.properties

# Arnés temporal de comparación de la revisión UX (Freebuff)
ux-check.html
ux-frame-proxy.mjs

# Extracción de frames del video de revisión (análisis local)
video-review/
video-review-deps/

# Exports de sesión de agentes (debug local)
new-session-*.json
